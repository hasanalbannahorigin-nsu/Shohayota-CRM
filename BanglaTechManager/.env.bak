# Postgres env variables used by docker-compose healthcheck
PGHOST=db
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=sohayota_crm

# Optional: keep DATABASE_URL for app if used
DATABASE_URL="postgres://postgres:postgres@db:5432/sohayota_crm"

# App secrets / config (আপনি চাইলে এগুলো পরিবর্তন করবেন)
SESSION_SECRET="please-change-this-to-a-strong-secret-32chars+"
NODE_ENV="production"
PORT=5000
ADMIN_PASSWORD="admin1234"
FRONTEND_URL="http://localhost:5000"
